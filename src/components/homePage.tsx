import NavBar from "./navBar";

const HomePage = (): JSX.Element => {
    return (
        <header>
            <NavBar />
            <h1>Hello WOrld</h1>
        </header>
    )
}

export default HomePage;